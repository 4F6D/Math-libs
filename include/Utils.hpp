#pragma once

static const float EPSILON = 0.0001f;	//- Tolerance of the euqality check of two floats

bool isEqual(float x, float y);
bool isGreaterThanOrEqual(float x, float y);
bool isLessThanOrEqual(float x, float y);